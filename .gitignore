# Rust
target/
**/target/
Cargo.lock

# Vendored deps + generated source-replacement config (from toolchain build attempts)
**/vendor/
**/.cargo/config.toml

# Node
node_modules/
.next/
dist/

# Python
.venv/
__pycache__/
*.pyc

# ink! build artifacts kept under contracts/*/target are ignored above

# Per-deploy run state (dev-node resets on restart, so addresses are ephemeral)
scripts/*_address.txt

# Local secrets
.env
*.local
