[metadata]
name = portal_name
version = 0.0.0
description = Python SDK for the Portal Naming Service (.pot) on Portaldot

[options]
packages = portal_name
python_requires = >=3.10
install_requires =
    substrate-interface==1.7.4
